//make sure to add this plugin in your code:
<plugin>
    <groupId>org.panteleyev</groupId>
    <artifactId>jpackage-maven-plugin</artifactId>
    <version>1.7.3</version> <configuration>
    <name>MyArtChatApp</name>
    <vendor>YourName</vendor>
    <appVersion>1.0.0</appVersion>

    <runtimeImage>target/image</runtimeImage>

    <module>com.example/com.example.MainApp</module>
    <destination>target/dist</destination>

    <type>MSI</type> <winDirChooser>true</winDirChooser> <winShortcut>true</winShortcut> <winMenu>true</winMenu> </configuration>
</plugin>

//make sure you've already installed WixToolset (v3 preferred)!

after that, try 
